{
  "name": "hashpoint",
  "version": "1.0.0",
  "description": "Offline-first crypto point-of-sale on HashKey Chain",
  "private": true,
  "workspaces": [
    "contracts",
    "sdk",
    "merchant-app",
    "backend"
  ],
  "dependencies": {
    "ethers": "^6.11.0"
  },
  "scripts": {
    "dev": "concurrently -n sdk,backend,app -c cyan,yellow,green \"npm run dev -w sdk\" \"npm run dev -w backend\" \"npm run dev -w merchant-app\"",
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present"
  },
  "overrides": {
    "ethers": "^6.11.0"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
